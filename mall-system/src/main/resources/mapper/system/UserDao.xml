<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.cmc.mall.module.system.dao.UserDao">

    <!-- 可根据自己的需求，是否要使用 -->
    <resultMap type="com.cmc.mall.module.system.entity.UserEntity" id="userMap">
        <result property="userId" column="id"/>
        <result property="username" column="username"/>
        <result property="password" column="password"/>
        <result property="salt" column="salt"/>
        <result property="email" column="email"/>
        <result property="mobile" column="mobile"/>
        <result property="createTime" column="create_time"/>
        <result property="createUserId" column="create_user_id"/>
        <result property="status" column="status"/>
    </resultMap>

    <select id="getUserByName" resultMap="userMap">
        select * from sys_user where username = #{username}
    </select>

    <!--查询用户的所有权限-->
    <select id="getUserPerms" resultType="java.lang.String">
    select sm.perms from sys_menu sm
    left join sys_role_menu srm on srm.menu_id=sm.menu_id
    left join sys_user_role sur on sur.role_id=srm.role_id
    where sur.user_id=#{userId}
    </select>
</mapper>